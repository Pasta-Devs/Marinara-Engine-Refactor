# Features

Feature-owned React code lives here. Each migrated feature should own its components, hooks, stores, local types, and helpers.

Feature code may import from `src/shared`, but shared code must not import feature code.
